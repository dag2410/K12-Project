const config = {
  routes: {
    home: "/",
    products: "/products",
    productDetail: "/products/:slug",
    register: "/register",
    login: "/login",
    users: "/users",
    notFound: "*",
  },
};
export default config;
